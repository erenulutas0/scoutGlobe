{
  "name": "@scoutglobe/etl",
  "version": "0.1.0",
  "private": true,
  "description": "Thin wrapper so Turborepo can lint the Python ETL jobs.",
  "scripts": {
    "lint": "uv run ruff check .",
    "format": "uv run ruff format .",
    "seed": "uv run python -m jobs.seed_reference",
    "test": "uv run pytest",
    "quality": "uv run python -m jobs.data_quality",
    "import:transfermarkt": "uv run python -m jobs.kaggle_transfermarkt && uv run python -m jobs.kaggle_matches && uv run python -m jobs.refresh_current_clubs",
    "refresh:clubs": "uv run python -m jobs.refresh_current_clubs",
    "squads:live": "uv run python -m jobs.apifootball_squads --league TR1 && uv run python -m jobs.merge_duplicate_players"
  }
}
